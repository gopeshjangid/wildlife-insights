mutation createOrganization($body: OrganizationCreate!) {
  createOrganization(body: $body) {
    id
    name
    streetAddress
    city
    state
    postalCode
    phone
    email
    countryCode
    organizationUrl
    remarks
  }
}
