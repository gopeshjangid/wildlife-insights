import { createAction } from 'vizzuality-redux-tools';

export const setFilters = createAction('subproject-deployments-list/setFilters');
export const reset = createAction('subproject-deployments-list/reset');
