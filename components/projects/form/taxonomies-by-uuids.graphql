query getTaxonomies($taxonomyUUIDs: [String]) {
  getTaxonomies(filters: { taxonomyUUIDs: $taxonomyUUIDs }) {
    data {
      id
      scientificName
      commonNameEnglish
      uniqueIdentifier
    }
  }
}
